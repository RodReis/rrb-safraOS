# MVPs e fatias

Cada `MVP-NNN.md` recorta o PRD em fatias verticais. Estrutura: resultado de negócio; dentro/fora; dependências/riscos; checklist de Slices; gate; métricas. Passo técnico não vira fatia. Reservar `SPEC/F` no `docs/STATUS.md` antes de numerar.

