# F4 — Cadastro web de fazendas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Owner cadastra, consulta, altera e arquiva fazendas do tenant ativo (nome, UF, município IBGE), isolado por RLS, com auditoria e tela conforme `docs/FRONTEND.md`.

**Architecture:** Domínio puro (`packages/backend/src/safraos/farms`) + repository/router FastAPI (`apps/api/src/safraos_api/modules/farms`) espelhando o padrão de `organizations`, mas com erro `application/problem+json` (novo). Migration Alembic cria `municipios` (seed, sem RLS) e `farms` (RLS FORCE). Frontend introduz cliente OpenAPI tipado (`openapi-typescript`+`openapi-fetch`+`openapi-react-query`), TanStack Query/Table, RHF+Zod, Sonner — infra nova usada só por farms nesta fatia.

**Tech Stack:** Python/FastAPI/SQLAlchemy async/Alembic, PostgreSQL/RLS, React 19/TypeScript/Vite, openapi-typescript, openapi-fetch, openapi-react-query, @tanstack/react-query, @tanstack/react-table, shadcn/ui DataTable, react-hook-form, zod, sonner, Playwright.

**Spec:** `docs/superpowers/specs/2026-09-14-f4-fazendas-design.md`

## Global Constraints

- UF: sigla de duas letras validada contra lista fixa das 27 UFs brasileiras.
- Município: código IBGE de 7 dígitos numéricos, exibido pelo nome; tabela `municipios` via seed na migration, sem hardcode em app code.
- Fazenda não contém CPF/CNPJ no MVP0.
- Exclusão física não entra: arquivamento (soft delete) preserva vínculos e auditoria.
- Nome de fazenda é obrigatório, normalizado, não presumido único.
- Toda tabela tenant-scoped tem `organization_id` FK+índice, RLS `ENABLE`+`FORCE`, policy default-deny.
- Toda ação sensível (criar/alterar/arquivar) grava `audit_events` (ator, tenant, ação, objeto, resultado, `correlationId`) na mesma transação.
- Erro HTTP de farms usa `application/problem+json` (`type`, `title`, `status`, `code`, `correlationId`); `organizations` continua com `MessageResponse` — **não mexer em organizations**.
- Frontend: `strict: true`, sem `any`; tipos de API vêm do OpenAPI gerado; schema Zod deriva do tipo da API; `npm run typecheck` falha se o gerado divergir do commitado (anti-drift).
- Toda mutação usa toast (Sonner): sucesso/erro; erro recuperável preserva dados do formulário.
- Estados loading (Skeleton)/empty/error/success visíveis e acessíveis; navegável por teclado.

---

## Task 1: Domínio puro de Farm

**Files:**
- Create: `packages/backend/src/safraos/farms/__init__.py`
- Create: `packages/backend/src/safraos/farms/model.py`
- Test: `tests/rules/farms/__init__.py`
- Test: `tests/rules/farms/test_farm_model.py`

**Interfaces:**
- Produces: `Farm` dataclass (`id: str`, `organization_id: str`, `name: str`, `uf: str`, `municipio_ibge_code: str`, `archived_at: datetime | None`), `create_farm(name: str, uf: str, municipio_ibge_code: str) -> tuple[str, str, str]` (retorna nome normalizado, UF, código validados — sem `id`/`organization_id`/timestamps, que são responsabilidade do repository), `FarmError(ValueError)` com atributo `code: str`, constante `BRAZILIAN_UF_CODES: frozenset[str]` com as 27 siglas.

- [ ] **Step 1: Escrever teste de domínio (nome, UF, IBGE)**

```python
# tests/rules/farms/test_farm_model.py
import pytest

from safraos.farms.model import BRAZILIAN_UF_CODES, FarmError, create_farm

pytestmark = pytest.mark.rules


def test_create_farm_normalizes_name_and_accepts_valid_uf_and_ibge():
    name, uf, ibge = create_farm("  Fazenda   Boa   Vista  ", "GO", "5208707")

    assert name == "Fazenda Boa Vista"
    assert uf == "GO"
    assert ibge == "5208707"


def test_create_farm_rejects_empty_name():
    with pytest.raises(FarmError) as error:
        create_farm("   ", "GO", "5208707")

    assert error.value.code == "farms.invalid_name"


def test_create_farm_rejects_unknown_uf():
    with pytest.raises(FarmError) as error:
        create_farm("Fazenda Teste", "XX", "5208707")

    assert error.value.code == "farms.invalid_uf"


def test_create_farm_uf_is_case_insensitive_and_normalized_to_upper():
    _, uf, _ = create_farm("Fazenda Teste", "go", "5208707")

    assert uf == "GO"


@pytest.mark.parametrize("bad_code", ["123", "abcdefg", "12345678", ""])
def test_create_farm_rejects_malformed_ibge_code(bad_code):
    with pytest.raises(FarmError) as error:
        create_farm("Fazenda Teste", "GO", bad_code)

    assert error.value.code == "farms.invalid_municipio"


def test_brazilian_uf_codes_has_27_entries():
    assert len(BRAZILIAN_UF_CODES) == 27
    assert "GO" in BRAZILIAN_UF_CODES
    assert "MT" in BRAZILIAN_UF_CODES
    assert "MS" in BRAZILIAN_UF_CODES
```

- [ ] **Step 2: Rodar teste e confirmar falha (módulo inexistente)**

Run: `python -m pytest tests/rules/farms/test_farm_model.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'safraos.farms'`

- [ ] **Step 3: Criar `__init__.py` do pacote de domínio**

```python
# packages/backend/src/safraos/farms/__init__.py
"""Dominio puro da entidade Farm (fazenda)."""
```

- [ ] **Step 4: Implementar `model.py`**

```python
# packages/backend/src/safraos/farms/model.py
"""Regras puras da entidade Farm: sem banco, rede ou relogio."""

import re

BRAZILIAN_UF_CODES: frozenset[str] = frozenset(
    {
        "AC", "AL", "AP", "AM", "BA", "CE", "DF", "ES", "GO",
        "MA", "MT", "MS", "MG", "PA", "PB", "PR", "PE", "PI",
        "RJ", "RN", "RS", "RO", "RR", "SC", "SP", "SE", "TO",
    }
)

_IBGE_CODE_PATTERN = re.compile(r"^\d{7}$")


class FarmError(ValueError):
    def __init__(self, code: str, message: str) -> None:
        super().__init__(message)
        self.code = code


def create_farm(name: str, uf: str, municipio_ibge_code: str) -> tuple[str, str, str]:
    normalized_name = " ".join(name.strip().split())
    if len(normalized_name) < 2:
        raise FarmError("farms.invalid_name", "Nome da fazenda deve ter ao menos 2 caracteres.")

    normalized_uf = uf.strip().upper()
    if normalized_uf not in BRAZILIAN_UF_CODES:
        raise FarmError("farms.invalid_uf", f"UF '{uf}' nao e uma sigla valida.")

    normalized_ibge = municipio_ibge_code.strip()
    if not _IBGE_CODE_PATTERN.match(normalized_ibge):
        raise FarmError(
            "farms.invalid_municipio",
            "Codigo IBGE do municipio deve ter 7 digitos numericos.",
        )

    return normalized_name, normalized_uf, normalized_ibge
```

- [ ] **Step 5: Criar `tests/rules/farms/__init__.py` vazio e rodar teste**

Run: `python -m pytest tests/rules/farms/test_farm_model.py -v`
Expected: PASS (7 testes)

- [ ] **Step 6: Commit**

```bash
git add packages/backend/src/safraos/farms tests/rules/farms
git commit -m "feat(farms): modele regras puras de fazenda"
```

---

## Task 2: Seed de municípios + migration de `farms`

**Files:**
- Create: `infra/alembic/versions/20260914_0004_farms.py`
- Create: `tests/database/test_farms_migration.py`

**Interfaces:**
- Consumes: role `safraos_app` e função `set_config('app.current_user_id', ...)` já criadas na migration `20260914_0003` (F3); tabela `organizations`, `organization_memberships`, `audit_events` já existentes.
- Produces: tabela `municipios` (`ibge_code char(7) PK`, `name text`, `uf char(2)`), tabela `farms` (`id uuid PK`, `organization_id uuid FK organizations`, `name text`, `uf char(2)`, `municipio_ibge_code char(7) FK municipios`, `archived_at timestamptz NULL`, `created_at timestamptz`).

- [ ] **Step 1: Escrever teste de banco para migration (RLS cross-tenant + seed + FK)**

```python
# tests/database/test_farms_migration.py
import uuid

import pytest
from sqlalchemy import text
from sqlalchemy.exc import DatabaseError, IntegrityError
from sqlalchemy.ext.asyncio import create_async_engine

from safraos_api.settings import Settings

pytestmark = [pytest.mark.database, pytest.mark.asyncio]


async def _truncate_all(engine):
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "TRUNCATE farms, organization_memberships, organizations, "
                "identity_users RESTART IDENTITY CASCADE"
            )
        )


async def test_municipios_seed_has_reference_data():
    engine = create_async_engine(Settings().database_url, pool_pre_ping=True)
    try:
        async with engine.begin() as conn:
            result = await conn.execute(
                text("SELECT name, uf FROM municipios WHERE ibge_code = :code"),
                {"code": "5208707"},
            )
            row = result.one()
            assert row.uf == "GO"
    finally:
        await engine.dispose()


async def test_farm_rejects_unknown_municipio_via_fk():
    owner_engine = create_async_engine(Settings().database_url, pool_pre_ping=True)
    try:
        await _truncate_all(owner_engine)
        user_id = str(uuid.uuid4())
        org_id = str(uuid.uuid4())
        async with owner_engine.begin() as conn:
            await conn.execute(
                text(
                    "INSERT INTO identity_users (id, email, password_hash, status) "
                    "VALUES (:id, :email, 'x', 'active')"
                ),
                {"id": user_id, "email": f"{user_id}@example.com"},
            )
            await conn.execute(
                text("INSERT INTO organizations (id, name) VALUES (:id, 'Org Teste')"),
                {"id": org_id},
            )
            with pytest.raises(IntegrityError):
                await conn.execute(
                    text(
                        "INSERT INTO farms (id, organization_id, name, uf, municipio_ibge_code) "
                        "VALUES (gen_random_uuid(), :org_id, 'Fazenda X', 'GO', '9999999')"
                    ),
                    {"org_id": org_id},
                )
    finally:
        await owner_engine.dispose()


async def test_rls_allows_only_member_organization_farms_through_app_role():
    owner_engine = create_async_engine(Settings().database_url, pool_pre_ping=True)
    app_engine = create_async_engine(Settings().app_database_url, pool_pre_ping=True)
    try:
        await _truncate_all(owner_engine)

        user_a, user_b = str(uuid.uuid4()), str(uuid.uuid4())
        org_a, org_b = str(uuid.uuid4()), str(uuid.uuid4())

        async with owner_engine.begin() as conn:
            for uid in (user_a, user_b):
                await conn.execute(
                    text(
                        "INSERT INTO identity_users (id, email, password_hash, status) "
                        "VALUES (:id, :email, 'x', 'active')"
                    ),
                    {"id": uid, "email": f"{uid}@example.com"},
                )
            for oid in (org_a, org_b):
                await conn.execute(
                    text("INSERT INTO organizations (id, name) VALUES (:id, 'Org')"),
                    {"id": oid},
                )
            await conn.execute(
                text(
                    "INSERT INTO organization_memberships (id, user_id, organization_id, role) "
                    "VALUES (gen_random_uuid(), :uid, :oid, 'owner')"
                ),
                {"uid": user_a, "oid": org_a},
            )
            await conn.execute(
                text(
                    "INSERT INTO organization_memberships (id, user_id, organization_id, role) "
                    "VALUES (gen_random_uuid(), :uid, :oid, 'owner')"
                ),
                {"uid": user_b, "oid": org_b},
            )
            await conn.execute(
                text(
                    "INSERT INTO farms (id, organization_id, name, uf, municipio_ibge_code) "
                    "VALUES (gen_random_uuid(), :oid, 'Fazenda A', 'GO', '5208707')"
                ),
                {"oid": org_a},
            )
            await conn.execute(
                text(
                    "INSERT INTO farms (id, organization_id, name, uf, municipio_ibge_code) "
                    "VALUES (gen_random_uuid(), :oid, 'Fazenda B', 'MT', '5103403')"
                ),
                {"oid": org_b},
            )

        async with app_engine.begin() as conn:
            await conn.execute(
                text("SELECT set_config('app.current_user_id', :uid, true)"),
                {"uid": user_a},
            )
            result = await conn.execute(text("SELECT name FROM farms"))
            names = {row.name for row in result}
            assert names == {"Fazenda A"}
    finally:
        await owner_engine.dispose()
        await app_engine.dispose()


async def test_farms_archive_is_update_not_delete_and_is_audited():
    owner_engine = create_async_engine(Settings().database_url, pool_pre_ping=True)
    try:
        await _truncate_all(owner_engine)
        with pytest.raises(DatabaseError):
            async with owner_engine.begin() as conn:
                await conn.execute(text("DELETE FROM audit_events"))
    finally:
        await owner_engine.dispose()
```

- [ ] **Step 2: Rodar teste e confirmar falha (tabela `farms`/`municipios` inexistente)**

Run: `python -m pytest tests/database/test_farms_migration.py -v`
Expected: FAIL com `UndefinedTable: relation "municipios" does not exist`

- [ ] **Step 3: Criar migration**

```python
# infra/alembic/versions/20260914_0004_farms.py
"""farms: seed de municipios, tabela farms com RLS."""

from alembic import op
import sqlalchemy as sa

revision = "20260914_0004"
down_revision = "20260914_0003"
branch_labels = None
depends_on = None

_MUNICIPIOS_SEED = [
    ("5208707", "Goiania", "GO"),
    ("5103403", "Cuiaba", "MT"),
    ("5002704", "Campo Grande", "MS"),
    ("5201405", "Aguas Lindas de Goias", "GO"),
    ("5107925", "Sinop", "MT"),
    ("5003207", "Dourados", "MS"),
]


def upgrade() -> None:
    op.create_table(
        "municipios",
        sa.Column("ibge_code", sa.CHAR(7), primary_key=True),
        sa.Column("name", sa.Text(), nullable=False),
        sa.Column("uf", sa.CHAR(2), nullable=False),
    )
    op.execute(
        sa.text(
            "GRANT SELECT ON municipios TO safraos_app;"
        )
    )
    municipios_table = sa.table(
        "municipios",
        sa.column("ibge_code", sa.CHAR(7)),
        sa.column("name", sa.Text()),
        sa.column("uf", sa.CHAR(2)),
    )
    op.bulk_insert(
        municipios_table,
        [
            {"ibge_code": code, "name": name, "uf": uf}
            for code, name, uf in _MUNICIPIOS_SEED
        ],
    )

    op.create_table(
        "farms",
        sa.Column(
            "id",
            sa.dialects.postgresql.UUID(as_uuid=False),
            primary_key=True,
            server_default=sa.text("gen_random_uuid()"),
        ),
        sa.Column(
            "organization_id",
            sa.dialects.postgresql.UUID(as_uuid=False),
            sa.ForeignKey("organizations.id"),
            nullable=False,
        ),
        sa.Column("name", sa.Text(), nullable=False),
        sa.Column("uf", sa.CHAR(2), nullable=False),
        sa.Column(
            "municipio_ibge_code",
            sa.CHAR(7),
            sa.ForeignKey("municipios.ibge_code"),
            nullable=False,
        ),
        sa.Column("archived_at", sa.TIMESTAMP(timezone=True), nullable=True),
        sa.Column(
            "created_at",
            sa.TIMESTAMP(timezone=True),
            nullable=False,
            server_default=sa.text("now()"),
        ),
    )
    op.create_index("ix_farms_organization_id", "farms", ["organization_id"])

    op.execute("ALTER TABLE farms ENABLE ROW LEVEL SECURITY;")
    op.execute("ALTER TABLE farms FORCE ROW LEVEL SECURITY;")

    op.execute(
        """
        CREATE POLICY farms_select_member ON farms
        FOR SELECT TO safraos_app
        USING (
            EXISTS (
                SELECT 1 FROM organization_memberships m
                WHERE m.organization_id = farms.organization_id
                  AND m.user_id = current_setting('app.current_user_id', true)::uuid
            )
        );
        """
    )
    op.execute(
        """
        CREATE POLICY farms_insert_member ON farms
        FOR INSERT TO safraos_app
        WITH CHECK (
            EXISTS (
                SELECT 1 FROM organization_memberships m
                WHERE m.organization_id = farms.organization_id
                  AND m.user_id = current_setting('app.current_user_id', true)::uuid
            )
        );
        """
    )
    op.execute(
        """
        CREATE POLICY farms_update_member ON farms
        FOR UPDATE TO safraos_app
        USING (
            EXISTS (
                SELECT 1 FROM organization_memberships m
                WHERE m.organization_id = farms.organization_id
                  AND m.user_id = current_setting('app.current_user_id', true)::uuid
            )
        )
        WITH CHECK (
            EXISTS (
                SELECT 1 FROM organization_memberships m
                WHERE m.organization_id = farms.organization_id
                  AND m.user_id = current_setting('app.current_user_id', true)::uuid
            )
        );
        """
    )

    op.execute("GRANT SELECT, INSERT, UPDATE ON farms TO safraos_app;")


def downgrade() -> None:
    op.execute("DROP POLICY IF EXISTS farms_update_member ON farms;")
    op.execute("DROP POLICY IF EXISTS farms_insert_member ON farms;")
    op.execute("DROP POLICY IF EXISTS farms_select_member ON farms;")
    op.drop_index("ix_farms_organization_id", table_name="farms")
    op.drop_table("farms")
    op.drop_table("municipios")
```

- [ ] **Step 4: Rodar migration local**

Run: `npm run dev` (garante Postgres de pé), depois `cd apps/api && uv run alembic upgrade head`
Expected: migration `20260914_0004` aplicada sem erro

- [ ] **Step 5: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/database/test_farms_migration.py -v`
Expected: PASS (4 testes)

- [ ] **Step 6: Commit**

```bash
git add infra/alembic/versions/20260914_0004_farms.py tests/database/test_farms_migration.py
git commit -m "feat(farms): crie tabela farms com rls e seed de municipios"
```

---

## Task 3: Handler global de erro `problem+json` (só para farms)

**Files:**
- Modify: `apps/api/src/safraos_api/main.py`
- Create: `apps/api/src/safraos_api/problem_details.py`
- Test: `tests/rules/api/test_problem_details.py`

**Interfaces:**
- Produces: `class ProblemDetailError(Exception)` com `status: int`, `title: str`, `code: str`; função `install_problem_detail_handler(app: FastAPI) -> None`; resposta JSON `{type, title, status, code, correlationId}`.
- Consumes: `request.state.correlation_id` (já setado pelo middleware existente de F1 — ver `apps/api/src/safraos_api/main.py`).

- [ ] **Step 1: Escrever teste do handler**

```python
# tests/rules/api/test_problem_details.py
import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient

from safraos_api.problem_details import ProblemDetailError, install_problem_detail_handler

pytestmark = pytest.mark.rules


def _build_app() -> FastAPI:
    app = FastAPI()
    install_problem_detail_handler(app)

    @app.middleware("http")
    async def _set_correlation(request, call_next):
        request.state.correlation_id = "test-correlation-id"
        return await call_next(request)

    @app.get("/boom")
    async def boom():
        raise ProblemDetailError(status=422, title="Nome invalido", code="farms.invalid_name")

    return app


def test_problem_detail_error_is_serialized_as_problem_json():
    client = TestClient(_build_app())

    response = client.get("/boom")

    assert response.status_code == 422
    assert response.headers["content-type"] == "application/problem+json"
    body = response.json()
    assert body == {
        "type": "about:blank",
        "title": "Nome invalido",
        "status": 422,
        "code": "farms.invalid_name",
        "correlationId": "test-correlation-id",
    }
```

- [ ] **Step 2: Rodar teste e confirmar falha**

Run: `python -m pytest tests/rules/api/test_problem_details.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'safraos_api.problem_details'`

- [ ] **Step 3: Implementar `problem_details.py`**

```python
# apps/api/src/safraos_api/problem_details.py
"""Handler de erro no formato application/problem+json (RFC 9457)."""

from fastapi import FastAPI, Request
from fastapi.responses import JSONResponse


class ProblemDetailError(Exception):
    def __init__(self, status: int, title: str, code: str) -> None:
        super().__init__(title)
        self.status = status
        self.title = title
        self.code = code


def install_problem_detail_handler(app: FastAPI) -> None:
    @app.exception_handler(ProblemDetailError)
    async def _handle_problem_detail(request: Request, exc: ProblemDetailError) -> JSONResponse:
        correlation_id = getattr(request.state, "correlation_id", None)
        return JSONResponse(
            status_code=exc.status,
            media_type="application/problem+json",
            content={
                "type": "about:blank",
                "title": exc.title,
                "status": exc.status,
                "code": exc.code,
                "correlationId": correlation_id,
            },
        )
```

- [ ] **Step 4: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/rules/api/test_problem_details.py -v`
Expected: PASS

- [ ] **Step 5: Registrar handler em `main.py`**

Ler `apps/api/src/safraos_api/main.py` primeiro para achar onde `app = FastAPI(...)` é criado e onde os routers existentes (`organizations_router`, etc.) são incluídos. Adicionar logo após a criação do `app`:

```python
from safraos_api.problem_details import install_problem_detail_handler

install_problem_detail_handler(app)
```

- [ ] **Step 6: Rodar suíte de regras completa**

Run: `python -m pytest tests/rules -v`
Expected: PASS, sem regressão em testes de organizations

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/safraos_api/problem_details.py apps/api/src/safraos_api/main.py tests/rules/api/test_problem_details.py
git commit -m "feat(api): adicione handler de erro problem+json para farms"
```

---

## Task 4: Repository de Farm

**Files:**
- Create: `apps/api/src/safraos_api/modules/farms/__init__.py`
- Create: `apps/api/src/safraos_api/modules/farms/repository.py`
- Test: `tests/database/test_farms_repository.py`

**Interfaces:**
- Consumes: `Settings` (`apps/api/src/safraos_api/settings.py`), `create_farm` (Task 1), `ProblemDetailError` (Task 3).
- Produces: `class FarmRepository` com métodos `async def create(self, user_id: str, organization_id: str, name: str, uf: str, municipio_ibge_code: str, correlation_id: str) -> FarmRow`, `async def list(self, user_id: str, organization_id: str, include_archived: bool) -> list[FarmRow]`, `async def update(self, user_id: str, organization_id: str, farm_id: str, name: str, uf: str, municipio_ibge_code: str, correlation_id: str) -> FarmRow`, `async def archive(self, user_id: str, organization_id: str, farm_id: str, correlation_id: str) -> FarmRow`; dataclass `FarmRow` (`id`, `organization_id`, `name`, `uf`, `municipio_ibge_code`, `municipio_name`, `archived_at`, `created_at`); função `build_repository(settings: Settings) -> FarmRepository` (mesmo padrão de `OrganizationRepository`).

- [ ] **Step 1: Escrever teste de integração do repository**

```python
# tests/database/test_farms_repository.py
import uuid

import pytest
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine

from safraos_api.modules.farms.repository import FarmRepository
from safraos_api.problem_details import ProblemDetailError
from safraos_api.settings import Settings

pytestmark = [pytest.mark.database, pytest.mark.asyncio]


async def _seed_user_and_org(owner_engine) -> tuple[str, str]:
    user_id = str(uuid.uuid4())
    org_id = str(uuid.uuid4())
    async with owner_engine.begin() as conn:
        await conn.execute(
            text(
                "TRUNCATE farms, organization_memberships, organizations, "
                "identity_users, audit_events RESTART IDENTITY CASCADE"
            )
        )
        await conn.execute(
            text(
                "INSERT INTO identity_users (id, email, password_hash, status) "
                "VALUES (:id, :email, 'x', 'active')"
            ),
            {"id": user_id, "email": f"{user_id}@example.com"},
        )
        await conn.execute(
            text("INSERT INTO organizations (id, name) VALUES (:id, 'Org Teste')"),
            {"id": org_id},
        )
        await conn.execute(
            text(
                "INSERT INTO organization_memberships (id, user_id, organization_id, role) "
                "VALUES (gen_random_uuid(), :uid, :oid, 'owner')"
            ),
            {"uid": user_id, "oid": org_id},
        )
    return user_id, org_id


async def test_create_list_update_archive_round_trip():
    settings = Settings()
    owner_engine = create_async_engine(settings.database_url, pool_pre_ping=True)
    repo = FarmRepository(create_async_engine(settings.app_database_url, pool_pre_ping=True))
    try:
        user_id, org_id = await _seed_user_and_org(owner_engine)

        created = await repo.create(
            user_id=user_id,
            organization_id=org_id,
            name="Fazenda Boa Vista",
            uf="GO",
            municipio_ibge_code="5208707",
            correlation_id="corr-1",
        )
        assert created.name == "Fazenda Boa Vista"
        assert created.archived_at is None

        listed = await repo.list(user_id=user_id, organization_id=org_id, include_archived=False)
        assert [f.id for f in listed] == [created.id]

        updated = await repo.update(
            user_id=user_id,
            organization_id=org_id,
            farm_id=created.id,
            name="Fazenda Boa Vista II",
            uf="GO",
            municipio_ibge_code="5208707",
            correlation_id="corr-2",
        )
        assert updated.name == "Fazenda Boa Vista II"

        archived = await repo.archive(
            user_id=user_id,
            organization_id=org_id,
            farm_id=created.id,
            correlation_id="corr-3",
        )
        assert archived.archived_at is not None

        active_only = await repo.list(user_id=user_id, organization_id=org_id, include_archived=False)
        assert active_only == []

        with_archived = await repo.list(user_id=user_id, organization_id=org_id, include_archived=True)
        assert len(with_archived) == 1
    finally:
        await owner_engine.dispose()
        await repo._engine.dispose()


async def test_create_rejects_invalid_uf_before_hitting_database():
    settings = Settings()
    owner_engine = create_async_engine(settings.database_url, pool_pre_ping=True)
    repo = FarmRepository(create_async_engine(settings.app_database_url, pool_pre_ping=True))
    try:
        user_id, org_id = await _seed_user_and_org(owner_engine)

        with pytest.raises(ProblemDetailError) as error:
            await repo.create(
                user_id=user_id,
                organization_id=org_id,
                name="Fazenda X",
                uf="ZZ",
                municipio_ibge_code="5208707",
                correlation_id="corr-1",
            )
        assert error.value.code == "farms.invalid_uf"
    finally:
        await owner_engine.dispose()
        await repo._engine.dispose()


async def test_update_of_foreign_organization_farm_raises_not_found():
    settings = Settings()
    owner_engine = create_async_engine(settings.database_url, pool_pre_ping=True)
    repo = FarmRepository(create_async_engine(settings.app_database_url, pool_pre_ping=True))
    try:
        user_a, org_a = await _seed_user_and_org(owner_engine)
        user_b_id = str(uuid.uuid4())
        org_b_id = str(uuid.uuid4())
        async with owner_engine.begin() as conn:
            await conn.execute(
                text(
                    "INSERT INTO identity_users (id, email, password_hash, status) "
                    "VALUES (:id, :email, 'x', 'active')"
                ),
                {"id": user_b_id, "email": f"{user_b_id}@example.com"},
            )
            await conn.execute(
                text("INSERT INTO organizations (id, name) VALUES (:id, 'Org B')"),
                {"id": org_b_id},
            )
            await conn.execute(
                text(
                    "INSERT INTO organization_memberships (id, user_id, organization_id, role) "
                    "VALUES (gen_random_uuid(), :uid, :oid, 'owner')"
                ),
                {"uid": user_b_id, "oid": org_b_id},
            )

        farm_a = await repo.create(
            user_id=user_a,
            organization_id=org_a,
            name="Fazenda A",
            uf="GO",
            municipio_ibge_code="5208707",
            correlation_id="corr-1",
        )

        with pytest.raises(ProblemDetailError) as error:
            await repo.update(
                user_id=user_b_id,
                organization_id=org_b_id,
                farm_id=farm_a.id,
                name="Hackeada",
                uf="GO",
                municipio_ibge_code="5208707",
                correlation_id="corr-2",
            )
        assert error.value.status == 404
    finally:
        await owner_engine.dispose()
        await repo._engine.dispose()
```

- [ ] **Step 2: Rodar teste e confirmar falha**

Run: `python -m pytest tests/database/test_farms_repository.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'safraos_api.modules.farms'`

- [ ] **Step 3: Implementar `repository.py`**

Ler primeiro `apps/api/src/safraos_api/modules/organizations/repository.py` inteiro para copiar exatamente o padrão de `_set_tenant_context`, `_audit_in_transaction` e `build_repository`. Adaptar:

```python
# apps/api/src/safraos_api/modules/farms/repository.py
"""Acesso a dados de Farm: uma transacao por caso de uso, RLS + auditoria."""

from dataclasses import dataclass
from datetime import datetime

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncConnection, AsyncEngine, create_async_engine

from safraos.farms.model import FarmError, create_farm
from safraos_api.problem_details import ProblemDetailError
from safraos_api.settings import Settings


@dataclass(frozen=True)
class FarmRow:
    id: str
    organization_id: str
    name: str
    uf: str
    municipio_ibge_code: str
    municipio_name: str
    archived_at: datetime | None
    created_at: datetime


async def _set_tenant_context(conn: AsyncConnection, user_id: str) -> None:
    await conn.execute(text("SELECT set_config('app.current_user_id', :user_id, true)"), {"user_id": user_id})


async def _audit_in_transaction(
    conn: AsyncConnection,
    *,
    actor_id: str,
    organization_id: str | None,
    action: str,
    object_id: str | None,
    outcome: str,
    correlation_id: str,
) -> None:
    await conn.execute(
        text(
            "INSERT INTO audit_events "
            "(id, actor_id, organization_id, action, object_id, outcome, correlation_id, created_at) "
            "VALUES (gen_random_uuid(), :actor_id, :organization_id, :action, :object_id, "
            ":outcome, :correlation_id, now())"
        ),
        {
            "actor_id": actor_id,
            "organization_id": organization_id,
            "action": action,
            "object_id": object_id,
            "outcome": outcome,
            "correlation_id": correlation_id,
        },
    )


def _row_to_farm(row) -> FarmRow:
    return FarmRow(
        id=str(row.id),
        organization_id=str(row.organization_id),
        name=row.name,
        uf=row.uf,
        municipio_ibge_code=row.municipio_ibge_code,
        municipio_name=row.municipio_name,
        archived_at=row.archived_at,
        created_at=row.created_at,
    )


_SELECT_WITH_MUNICIPIO = """
    SELECT f.id, f.organization_id, f.name, f.uf, f.municipio_ibge_code,
           m.name AS municipio_name, f.archived_at, f.created_at
    FROM farms f
    JOIN municipios m ON m.ibge_code = f.municipio_ibge_code
"""


class FarmRepository:
    def __init__(self, engine: AsyncEngine) -> None:
        self._engine = engine

    async def create(
        self,
        *,
        user_id: str,
        organization_id: str,
        name: str,
        uf: str,
        municipio_ibge_code: str,
        correlation_id: str,
    ) -> FarmRow:
        try:
            normalized_name, normalized_uf, normalized_ibge = create_farm(name, uf, municipio_ibge_code)
        except FarmError as error:
            raise ProblemDetailError(status=422, title=str(error), code=error.code) from error

        async with self._engine.begin() as conn:
            await _set_tenant_context(conn, user_id)
            result = await conn.execute(
                text(
                    "INSERT INTO farms (id, organization_id, name, uf, municipio_ibge_code) "
                    "VALUES (gen_random_uuid(), :organization_id, :name, :uf, :ibge) "
                    "RETURNING id"
                ),
                {
                    "organization_id": organization_id,
                    "name": normalized_name,
                    "uf": normalized_uf,
                    "ibge": normalized_ibge,
                },
            )
            farm_id = str(result.scalar_one())
            await _audit_in_transaction(
                conn,
                actor_id=user_id,
                organization_id=organization_id,
                action="farms.create",
                object_id=farm_id,
                outcome="allowed",
                correlation_id=correlation_id,
            )
            row = (
                await conn.execute(
                    text(_SELECT_WITH_MUNICIPIO + " WHERE f.id = :id"),
                    {"id": farm_id},
                )
            ).one()
            return _row_to_farm(row)

    async def list(
        self, *, user_id: str, organization_id: str, include_archived: bool
    ) -> list[FarmRow]:
        query = _SELECT_WITH_MUNICIPIO + " WHERE f.organization_id = :organization_id"
        if not include_archived:
            query += " AND f.archived_at IS NULL"
        query += " ORDER BY f.name"

        async with self._engine.begin() as conn:
            await _set_tenant_context(conn, user_id)
            result = await conn.execute(text(query), {"organization_id": organization_id})
            return [_row_to_farm(row) for row in result]

    async def update(
        self,
        *,
        user_id: str,
        organization_id: str,
        farm_id: str,
        name: str,
        uf: str,
        municipio_ibge_code: str,
        correlation_id: str,
    ) -> FarmRow:
        try:
            normalized_name, normalized_uf, normalized_ibge = create_farm(name, uf, municipio_ibge_code)
        except FarmError as error:
            raise ProblemDetailError(status=422, title=str(error), code=error.code) from error

        async with self._engine.begin() as conn:
            await _set_tenant_context(conn, user_id)
            result = await conn.execute(
                text(
                    "UPDATE farms SET name = :name, uf = :uf, municipio_ibge_code = :ibge "
                    "WHERE id = :id AND organization_id = :organization_id "
                    "RETURNING id"
                ),
                {
                    "name": normalized_name,
                    "uf": normalized_uf,
                    "ibge": normalized_ibge,
                    "id": farm_id,
                    "organization_id": organization_id,
                },
            )
            updated_id = result.scalar_one_or_none()
            if updated_id is None:
                await _audit_in_transaction(
                    conn,
                    actor_id=user_id,
                    organization_id=organization_id,
                    action="farms.update",
                    object_id=farm_id,
                    outcome="denied",
                    correlation_id=correlation_id,
                )
                raise ProblemDetailError(status=404, title="Fazenda nao encontrada.", code="farms.not_found")

            await _audit_in_transaction(
                conn,
                actor_id=user_id,
                organization_id=organization_id,
                action="farms.update",
                object_id=farm_id,
                outcome="allowed",
                correlation_id=correlation_id,
            )
            row = (
                await conn.execute(
                    text(_SELECT_WITH_MUNICIPIO + " WHERE f.id = :id"),
                    {"id": farm_id},
                )
            ).one()
            return _row_to_farm(row)

    async def archive(
        self, *, user_id: str, organization_id: str, farm_id: str, correlation_id: str
    ) -> FarmRow:
        async with self._engine.begin() as conn:
            await _set_tenant_context(conn, user_id)
            result = await conn.execute(
                text(
                    "UPDATE farms SET archived_at = now() "
                    "WHERE id = :id AND organization_id = :organization_id AND archived_at IS NULL "
                    "RETURNING id"
                ),
                {"id": farm_id, "organization_id": organization_id},
            )
            archived_id = result.scalar_one_or_none()
            if archived_id is None:
                await _audit_in_transaction(
                    conn,
                    actor_id=user_id,
                    organization_id=organization_id,
                    action="farms.archive",
                    object_id=farm_id,
                    outcome="denied",
                    correlation_id=correlation_id,
                )
                raise ProblemDetailError(status=404, title="Fazenda nao encontrada.", code="farms.not_found")

            await _audit_in_transaction(
                conn,
                actor_id=user_id,
                organization_id=organization_id,
                action="farms.archive",
                object_id=farm_id,
                outcome="allowed",
                correlation_id=correlation_id,
            )
            row = (
                await conn.execute(
                    text(_SELECT_WITH_MUNICIPIO + " WHERE f.id = :id"),
                    {"id": farm_id},
                )
            ).one()
            return _row_to_farm(row)


def build_repository(settings: Settings) -> FarmRepository:
    engine = create_async_engine(settings.app_database_url, pool_pre_ping=True)
    return FarmRepository(engine)
```

**Nota de implementação:** conferir o nome exato das colunas de `audit_events` lendo a migration `20260914_0003_organizations_tenancy_audit.py` antes de escrever o INSERT acima — usar os mesmos nomes de coluna que `OrganizationRepository._audit_in_transaction` já usa, ajustando só se o nome divergir do que está neste plano.

- [ ] **Step 4: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/database/test_farms_repository.py -v`
Expected: PASS (3 testes)

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/safraos_api/modules/farms/__init__.py apps/api/src/safraos_api/modules/farms/repository.py tests/database/test_farms_repository.py
git commit -m "feat(farms): implemente repository com tenant context e auditoria"
```

---

## Task 5: Router de Farm (API HTTP)

**Files:**
- Create: `apps/api/src/safraos_api/modules/farms/router.py`
- Modify: `apps/api/src/safraos_api/main.py`
- Test: `tests/rules/api/test_farms_router.py`

**Interfaces:**
- Consumes: `FarmRepository`, `FarmRow`, `build_repository` (Task 4), `ProblemDetailError` (Task 3), `CurrentUserDep`/`current_user` de `apps/api/src/safraos_api/modules/organizations/router.py` (reaproveitar a dependency de sessão — ler o arquivo antes de decidir se importa direto ou duplica; se `current_user` depender só de `OrganizationRepository.session_user`, extrair para um módulo compartilhado é preferível a duplicar, mas **só faça essa extração se for trivial**; senão, duplique a dependency de sessão dentro de `modules/farms/router.py` lendo `SessionUser` do mesmo jeito).
- Produces: `APIRouter` em `/v1/farms` com `POST /`, `GET /`, `PUT /{farm_id}`, `POST /{farm_id}/archive`; Pydantic models `FarmCreateRequest`, `FarmUpdateRequest`, `FarmResponse`.

- [ ] **Step 1: Escrever teste do router com repository fake**

```python
# tests/rules/api/test_farms_router.py
from datetime import datetime, timezone

import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient

from safraos_api.modules.farms.repository import FarmRow
from safraos_api.modules.farms.router import get_repository, router
from safraos_api.problem_details import ProblemDetailError, install_problem_detail_handler

pytestmark = pytest.mark.rules


class FakeFarmRepository:
    def __init__(self) -> None:
        self.farms: dict[str, FarmRow] = {}

    async def create(self, *, user_id, organization_id, name, uf, municipio_ibge_code, correlation_id):
        farm_id = f"farm-{len(self.farms) + 1}"
        row = FarmRow(
            id=farm_id,
            organization_id=organization_id,
            name=name,
            uf=uf,
            municipio_ibge_code=municipio_ibge_code,
            municipio_name="Goiania",
            archived_at=None,
            created_at=datetime.now(timezone.utc),
        )
        self.farms[farm_id] = row
        return row

    async def list(self, *, user_id, organization_id, include_archived):
        return [
            f for f in self.farms.values()
            if f.organization_id == organization_id and (include_archived or f.archived_at is None)
        ]

    async def update(self, *, user_id, organization_id, farm_id, name, uf, municipio_ibge_code, correlation_id):
        existing = self.farms.get(farm_id)
        if existing is None or existing.organization_id != organization_id:
            raise ProblemDetailError(status=404, title="Fazenda nao encontrada.", code="farms.not_found")
        updated = FarmRow(
            id=farm_id,
            organization_id=organization_id,
            name=name,
            uf=uf,
            municipio_ibge_code=municipio_ibge_code,
            municipio_name=existing.municipio_name,
            archived_at=existing.archived_at,
            created_at=existing.created_at,
        )
        self.farms[farm_id] = updated
        return updated

    async def archive(self, *, user_id, organization_id, farm_id, correlation_id):
        existing = self.farms.get(farm_id)
        if existing is None or existing.organization_id != organization_id:
            raise ProblemDetailError(status=404, title="Fazenda nao encontrada.", code="farms.not_found")
        archived = FarmRow(
            id=farm_id,
            organization_id=organization_id,
            name=existing.name,
            uf=existing.uf,
            municipio_ibge_code=existing.municipio_ibge_code,
            municipio_name=existing.municipio_name,
            archived_at=datetime.now(timezone.utc),
            created_at=existing.created_at,
        )
        self.farms[farm_id] = archived
        return archived


def _build_app(fake_repo: FakeFarmRepository) -> FastAPI:
    app = FastAPI()
    install_problem_detail_handler(app)

    @app.middleware("http")
    async def _set_correlation(request, call_next):
        request.state.correlation_id = "test-correlation-id"
        return await call_next(request)

    app.include_router(router)
    app.dependency_overrides[get_repository] = lambda: fake_repo
    return app


def test_create_requires_active_organization_cookie():
    app = _build_app(FakeFarmRepository())
    client = TestClient(app)

    response = client.post("/v1/farms", json={"name": "Fazenda X", "uf": "GO", "municipioIbgeCode": "5208707"})

    assert response.status_code == 401


def test_create_and_list_farm():
    app = _build_app(FakeFarmRepository())
    client = TestClient(app)
    client.cookies.set("safraos_session", "fake-session")
    client.cookies.set("safraos_active_organization", "org-1")

    create_response = client.post(
        "/v1/farms", json={"name": "Fazenda X", "uf": "GO", "municipioIbgeCode": "5208707"}
    )
    assert create_response.status_code == 201

    list_response = client.get("/v1/farms")
    assert list_response.status_code == 200
    assert len(list_response.json()["items"]) == 1


def test_archive_of_unknown_farm_returns_problem_json_404():
    app = _build_app(FakeFarmRepository())
    client = TestClient(app)
    client.cookies.set("safraos_session", "fake-session")
    client.cookies.set("safraos_active_organization", "org-1")

    response = client.post("/v1/farms/does-not-exist/archive")

    assert response.status_code == 404
    assert response.headers["content-type"] == "application/problem+json"
    assert response.json()["code"] == "farms.not_found"
```

**Nota de implementação:** o teste acima assume que a dependency de sessão em `modules/farms/router.py` resolve `SessionUser` a partir do cookie `safraos_session` via alguma implementação fake/injetável nos testes — ler exatamente como `tests/rules/api/test_organizations_router.py` mocka sessão (provavelmente via `FakeOrganizationRepository.session_user`) e replicar o mesmo mecanismo aqui, ajustando os testes acima se o mock de sessão do repository real for diferente do que este plano assume.

- [ ] **Step 2: Rodar teste e confirmar falha**

Run: `python -m pytest tests/rules/api/test_farms_router.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'safraos_api.modules.farms.router'`

- [ ] **Step 3: Implementar `router.py`**

Ler `apps/api/src/safraos_api/modules/organizations/router.py` inteiro primeiro e copiar o padrão de `CurrentUserDep`, `RepositoryDep`, cookie `safraos_active_organization`, `request.state.correlation_id`. Adaptar:

```python
# apps/api/src/safraos_api/modules/farms/router.py
"""Rotas HTTP de Farm: autorizacao de sessao + tenant ativo + problem+json."""

from typing import Annotated

from fastapi import APIRouter, Depends, Request
from pydantic import BaseModel, Field

from safraos_api.modules.farms.repository import FarmRepository, FarmRow, build_repository
from safraos_api.modules.organizations.router import CurrentUserDep
from safraos_api.problem_details import ProblemDetailError
from safraos_api.settings import Settings

router = APIRouter(prefix="/v1/farms", tags=["farms"])


def get_repository() -> FarmRepository:
    return build_repository(Settings())


RepositoryDep = Annotated[FarmRepository, Depends(get_repository)]


class FarmCreateRequest(BaseModel):
    name: str
    uf: str
    municipio_ibge_code: str = Field(alias="municipioIbgeCode")

    model_config = {"populate_by_name": True}


class FarmUpdateRequest(FarmCreateRequest):
    pass


class FarmResponse(BaseModel):
    id: str
    name: str
    uf: str
    municipio_ibge_code: str = Field(alias="municipioIbgeCode")
    municipio_name: str = Field(alias="municipioName")
    archived_at: str | None = Field(alias="archivedAt")

    model_config = {"populate_by_name": True}

    @staticmethod
    def from_row(row: FarmRow) -> "FarmResponse":
        return FarmResponse(
            id=row.id,
            name=row.name,
            uf=row.uf,
            municipioIbgeCode=row.municipio_ibge_code,
            municipioName=row.municipio_name,
            archivedAt=row.archived_at.isoformat() if row.archived_at else None,
        )


class FarmListResponse(BaseModel):
    items: list[FarmResponse]


def _require_active_organization(request: Request) -> str:
    organization_id = request.cookies.get("safraos_active_organization")
    if not organization_id:
        raise ProblemDetailError(status=401, title="Nenhum tenant ativo.", code="farms.no_active_tenant")
    return organization_id


@router.post("", response_model=FarmResponse, status_code=201)
async def create_farm_endpoint(
    payload: FarmCreateRequest,
    request: Request,
    user: CurrentUserDep,
    repo: RepositoryDep,
):
    if user is None:
        raise ProblemDetailError(status=401, title="Sessao invalida.", code="farms.unauthorized")
    organization_id = _require_active_organization(request)
    row = await repo.create(
        user_id=user.id,
        organization_id=organization_id,
        name=payload.name,
        uf=payload.uf,
        municipio_ibge_code=payload.municipio_ibge_code,
        correlation_id=request.state.correlation_id,
    )
    return FarmResponse.from_row(row)


@router.get("", response_model=FarmListResponse)
async def list_farms_endpoint(
    request: Request,
    user: CurrentUserDep,
    repo: RepositoryDep,
    include_archived: bool = False,
):
    if user is None:
        raise ProblemDetailError(status=401, title="Sessao invalida.", code="farms.unauthorized")
    organization_id = _require_active_organization(request)
    rows = await repo.list(user_id=user.id, organization_id=organization_id, include_archived=include_archived)
    return FarmListResponse(items=[FarmResponse.from_row(r) for r in rows])


@router.put("/{farm_id}", response_model=FarmResponse)
async def update_farm_endpoint(
    farm_id: str,
    payload: FarmUpdateRequest,
    request: Request,
    user: CurrentUserDep,
    repo: RepositoryDep,
):
    if user is None:
        raise ProblemDetailError(status=401, title="Sessao invalida.", code="farms.unauthorized")
    organization_id = _require_active_organization(request)
    row = await repo.update(
        user_id=user.id,
        organization_id=organization_id,
        farm_id=farm_id,
        name=payload.name,
        uf=payload.uf,
        municipio_ibge_code=payload.municipio_ibge_code,
        correlation_id=request.state.correlation_id,
    )
    return FarmResponse.from_row(row)


@router.post("/{farm_id}/archive", response_model=FarmResponse)
async def archive_farm_endpoint(
    farm_id: str,
    request: Request,
    user: CurrentUserDep,
    repo: RepositoryDep,
):
    if user is None:
        raise ProblemDetailError(status=401, title="Sessao invalida.", code="farms.unauthorized")
    organization_id = _require_active_organization(request)
    row = await repo.archive(
        user_id=user.id,
        organization_id=organization_id,
        farm_id=farm_id,
        correlation_id=request.state.correlation_id,
    )
    return FarmResponse.from_row(row)
```

**Nota de implementação:** `CurrentUserDep` está sendo importado de `modules/organizations/router.py`. Ler esse arquivo primeiro — se `CurrentUserDep` depender de `OrganizationRepository` especificamente (não genérico), não dá pra reaproveitar direto. Nesse caso, duplicar a dependency de sessão dentro de `modules/farms/router.py` usando `FarmRepository`/uma query própria de sessão (replicar `session_user` também no `FarmRepository`, ou extrair um `SessionRepository` compartilhado se isso não for grande demais para esta tarefa — decisão de implementação, registrar na PR).

- [ ] **Step 4: Registrar router em `main.py`**

Ler `apps/api/src/safraos_api/main.py`, achar onde `organizations_router` é incluído (`app.include_router(...)`), adicionar linha equivalente para o router de farms.

- [ ] **Step 5: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/rules/api/test_farms_router.py -v`
Expected: PASS (3 testes)

- [ ] **Step 6: Rodar suíte de regras completa**

Run: `python -m pytest tests/rules -v`
Expected: PASS, sem regressão

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/safraos_api/modules/farms/router.py apps/api/src/safraos_api/main.py tests/rules/api/test_farms_router.py
git commit -m "feat(farms): exponha rotas HTTP de fazenda com problem+json"
```

---

## Task 6: Geração de tipos OpenAPI + script anti-drift

**Files:**
- Create: `scripts/generate-openapi.mjs`
- Create: `apps/web/src/lib/api-types.ts` (gerado, commitado)
- Modify: `scripts/typecheck.mjs`
- Modify: `package.json` (devDependency `openapi-typescript`)
- Modify: `apps/web/package.json` (dependencies `openapi-fetch`, `openapi-react-query`)

**Interfaces:**
- Produces: script `npm run generate:openapi` que escreve `apps/web/src/lib/api-types.ts`; `scripts/typecheck.mjs` roda esse script antes do `tsc` e falha (`process.exit(1)`) se o arquivo gerado divergir do commitado (`git diff --exit-code` sobre o arquivo, ou comparação de conteúdo).

- [ ] **Step 1: Instalar dependências**

Run: `npm install -D openapi-typescript` (raiz) e `cd apps/web && npm install openapi-fetch openapi-react-query`
Expected: `package.json`/`apps/web/package.json` e lockfile atualizados

- [ ] **Step 2: Ler `scripts/typecheck.mjs` e `scripts/dev.mjs` para entender o wrapper de spawn portátil (Windows `.cmd`)**

Antes de escrever `generate-openapi.mjs`, ler `scripts/typecheck.mjs` e algum script irmão (ex: `scripts/lint.mjs`) para replicar exatamente o helper de `spawn`/`toSpawnArgs` já testado em `tests/rules` (ver Task 1 do F1 — `toSpawnArgs — portabilidade dos wrappers`). Não reescrever esse helper; importar de onde ele já vive.

- [ ] **Step 3: Escrever `scripts/generate-openapi.mjs`**

```javascript
// scripts/generate-openapi.mjs
import { existsSync, readFileSync } from "node:fs";
import { resolve } from "node:path";

import openapiTS, { astToString } from "openapi-typescript";

const API_BASE_URL = process.env.VITE_API_BASE_URL ?? "http://localhost:5183";
const OUTPUT_PATH = resolve("apps/web/src/lib/api-types.ts");

async function main() {
  const schemaUrl = `${API_BASE_URL}/openapi.json`;
  console.log(`[safraos] generate:openapi: buscando schema em ${schemaUrl}`);

  const ast = await openapiTS(new URL(schemaUrl));
  const output = astToString(ast);

  const checkOnly = process.argv.includes("--check");
  if (checkOnly) {
    if (!existsSync(OUTPUT_PATH)) {
      console.error("[safraos] generate:openapi: arquivo gerado ausente — rode `npm run generate:openapi`.");
      process.exit(1);
    }
    const committed = readFileSync(OUTPUT_PATH, "utf8");
    if (committed !== output) {
      console.error(
        "[safraos] generate:openapi: apps/web/src/lib/api-types.ts esta desatualizado. " +
          "Rode `npm run generate:openapi` e commite o resultado."
      );
      process.exit(1);
    }
    console.log("[safraos] generate:openapi: OK, sem drift.");
    return;
  }

  const { writeFileSync } = await import("node:fs");
  writeFileSync(OUTPUT_PATH, output);
  console.log(`[safraos] generate:openapi: escrito em ${OUTPUT_PATH}`);
}

main().catch((error) => {
  console.error("[safraos] generate:openapi: falhou", error);
  process.exit(1);
});
```

- [ ] **Step 4: Adicionar script `generate:openapi` em `package.json`**

```json
"generate:openapi": "node scripts/generate-openapi.mjs"
```

- [ ] **Step 5: Ler `scripts/typecheck.mjs` completo e adicionar chamada anti-drift antes do typecheck de `apps/web`**

Adicionar, na etapa que já roda `[safraos] typecheck: apps/web` (antes de `tsc -b --noEmit`), uma chamada equivalente a `node scripts/generate-openapi.mjs --check` (via o mesmo helper de `spawn` do arquivo) — a API precisa estar rodando (`npm run dev` de pé) para esse passo funcionar; documentar essa dependência no próprio script (mensagem de erro clara se a API não responder).

- [ ] **Step 6: Gerar o arquivo pela primeira vez (API precisa estar rodando)**

Run: `npm run dev` (esperar health checks), depois `npm run generate:openapi`
Expected: `apps/web/src/lib/api-types.ts` criado com tipos de `paths`/`components` incluindo `/v1/farms`

- [ ] **Step 7: Rodar `npm run typecheck` e confirmar que o anti-drift passa (arquivo recém-gerado bate com ele mesmo)**

Run: `npm run typecheck`
Expected: PASS, incluindo a nova etapa de check do OpenAPI

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json apps/web/package.json apps/web/package-lock.json scripts/generate-openapi.mjs scripts/typecheck.mjs apps/web/src/lib/api-types.ts
git commit -m "feat(web): gere tipos OpenAPI com verificacao anti-drift no typecheck"
```

---

## Task 7: Cliente `$api` (openapi-fetch + openapi-react-query) e `QueryClientProvider`

**Files:**
- Create: `apps/web/src/lib/apiClient.ts`
- Modify: `apps/web/src/App.tsx`
- Test: `apps/web/src/lib/apiClient.test.ts`

**Interfaces:**
- Consumes: `apps/web/src/lib/api-types.ts` (Task 6, tipo `paths`).
- Produces: `export const $api` (instância de `openapi-react-query`); `export const queryClient` (instância de `QueryClient` do TanStack Query, exportada para uso em testes e no `App.tsx`).

- [ ] **Step 1: Instalar `@tanstack/react-query`**

Run: `cd apps/web && npm install @tanstack/react-query`

- [ ] **Step 2: Escrever teste do cliente**

```typescript
// apps/web/src/lib/apiClient.test.ts
import { describe, expect, it } from "vitest";

import { $api, queryClient } from "./apiClient";

describe("apiClient", () => {
  it("expoe useQuery e useMutation tipados", () => {
    expect(typeof $api.useQuery).toBe("function");
    expect(typeof $api.useMutation).toBe("function");
  });

  it("expoe uma instancia de QueryClient compartilhada", () => {
    expect(queryClient.getQueryCache).toBeDefined();
  });
});
```

- [ ] **Step 3: Rodar teste e confirmar falha**

Run: `cd apps/web && npx vitest run src/lib/apiClient.test.ts`
Expected: FAIL com `Cannot find module './apiClient'`

- [ ] **Step 4: Implementar `apiClient.ts`**

```typescript
// apps/web/src/lib/apiClient.ts
import { QueryClient } from "@tanstack/react-query";
import createFetchClient from "openapi-fetch";
import createClient from "openapi-react-query";

import type { paths } from "./api-types";

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? "http://localhost:5183";

const fetchClient = createFetchClient<paths>({
  baseUrl: API_BASE_URL,
  credentials: "include",
});

export const $api = createClient(fetchClient);

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 30_000,
      refetchOnWindowFocus: false,
    },
  },
});
```

- [ ] **Step 5: Rodar teste e confirmar passagem**

Run: `cd apps/web && npx vitest run src/lib/apiClient.test.ts`
Expected: PASS

- [ ] **Step 6: Envolver o shell com `QueryClientProvider`**

Ler `apps/web/src/App.tsx` inteiro primeiro. Envolver a árvore existente com:

```tsx
import { QueryClientProvider } from "@tanstack/react-query";

import { queryClient } from "./lib/apiClient";

// dentro do componente App, envolver o retorno existente:
// <QueryClientProvider client={queryClient}>{...conteudo atual...}</QueryClientProvider>
```

- [ ] **Step 7: Rodar suíte web completa**

Run: `cd apps/web && npx vitest run`
Expected: PASS, sem regressão nos testes de `AuthPanel`/`OrganizationPanel`

- [ ] **Step 8: Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json apps/web/src/lib/apiClient.ts apps/web/src/lib/apiClient.test.ts apps/web/src/App.tsx
git commit -m "feat(web): configure cliente openapi-react-query e QueryClientProvider"
```

---

## Task 8: `DataTable` genérico reaproveitável

**Files:**
- Create: `packages/frontend/src/components/DataTable.tsx`
- Test: `packages/frontend/src/components/DataTable.test.tsx`

**Interfaces:**
- Consumes: `@tanstack/react-table` (`ColumnDef`, `useReactTable`, `getCoreRowModel`, `getSortedRowModel`, `getFilteredRowModel`).
- Produces: `interface DataTableProps<TData>` (`columns: ColumnDef<TData, unknown>[]`, `data: TData[]`, `emptyMessage: string`, `getRowId?: (row: TData) => string`, `onRowClick?: (row: TData) => void`); `export function DataTable<TData>(props: DataTableProps<TData>): JSX.Element`. Sem paginação/filtro server-side nesta fatia (client-side, conforme spec) — mas a ordenação por coluna e um campo de busca client-side fazem parte do componente.

- [ ] **Step 1: Confirmar se `packages/frontend` já tem `package.json`/build próprio**

Run: `cat packages/frontend/package.json` (ou equivalente) — se não existir ainda, criar um mínimo com `"name": "@safraos/frontend"`, `"main"`/`"types"` apontando pro `src`, e adicionar como dependência workspace em `apps/web/package.json` (`"@safraos/frontend": "*"` já deve existir pelo `workspaces` do `package.json` raiz — conferir).

- [ ] **Step 2: Instalar `@tanstack/react-table`**

Run: `cd apps/web && npm install @tanstack/react-table` (ou na raiz de `packages/frontend`, conforme onde as dependências de workspace já resolvem — seguir o padrão que `apps/web/package.json` já usa para dependências compartilhadas)

- [ ] **Step 3: Escrever teste do `DataTable`**

```tsx
// packages/frontend/src/components/DataTable.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import type { ColumnDef } from "@tanstack/react-table";

import { DataTable } from "./DataTable";

interface Row {
  id: string;
  name: string;
}

const columns: ColumnDef<Row, unknown>[] = [
  { accessorKey: "name", header: "Nome" },
];

describe("DataTable", () => {
  it("renderiza linhas de dados", () => {
    render(
      <DataTable
        columns={columns}
        data={[{ id: "1", name: "Fazenda A" }]}
        emptyMessage="Nenhuma fazenda."
        getRowId={(row) => row.id}
      />
    );

    expect(screen.getByText("Fazenda A")).toBeInTheDocument();
  });

  it("mostra mensagem de vazio quando nao ha dados", () => {
    render(<DataTable columns={columns} data={[]} emptyMessage="Nenhuma fazenda." />);

    expect(screen.getByText("Nenhuma fazenda.")).toBeInTheDocument();
  });

  it("chama onRowClick ao clicar na linha", () => {
    const onRowClick = vi.fn();
    render(
      <DataTable
        columns={columns}
        data={[{ id: "1", name: "Fazenda A" }]}
        emptyMessage="Nenhuma fazenda."
        getRowId={(row) => row.id}
        onRowClick={onRowClick}
      />
    );

    screen.getByText("Fazenda A").closest("tr")?.click();
    expect(onRowClick).toHaveBeenCalledWith({ id: "1", name: "Fazenda A" });
  });
});
```

- [ ] **Step 4: Rodar teste e confirmar falha**

Run: `cd apps/web && npx vitest run ../../packages/frontend/src/components/DataTable.test.tsx` (ajustar path conforme runner/config de workspace resolvido no Step 1)
Expected: FAIL com `Cannot find module './DataTable'`

- [ ] **Step 5: Implementar `DataTable.tsx`**

```tsx
// packages/frontend/src/components/DataTable.tsx
import {
  type ColumnDef,
  flexRender,
  getCoreRowModel,
  getSortedRowModel,
  useReactTable,
} from "@tanstack/react-table";

export interface DataTableProps<TData> {
  columns: ColumnDef<TData, unknown>[];
  data: TData[];
  emptyMessage: string;
  getRowId?: (row: TData) => string;
  onRowClick?: (row: TData) => void;
}

export function DataTable<TData>({
  columns,
  data,
  emptyMessage,
  getRowId,
  onRowClick,
}: DataTableProps<TData>) {
  const table = useReactTable({
    data,
    columns,
    getCoreRowModel: getCoreRowModel(),
    getSortedRowModel: getSortedRowModel(),
    getRowId: getRowId ? (row) => getRowId(row) : undefined,
  });

  if (data.length === 0) {
    return <p role="status">{emptyMessage}</p>;
  }

  return (
    <table>
      <thead>
        {table.getHeaderGroups().map((headerGroup) => (
          <tr key={headerGroup.id}>
            {headerGroup.headers.map((header) => (
              <th key={header.id}>
                {header.isPlaceholder
                  ? null
                  : flexRender(header.column.columnDef.header, header.getContext())}
              </th>
            ))}
          </tr>
        ))}
      </thead>
      <tbody>
        {table.getRowModel().rows.map((row) => (
          <tr
            key={row.id}
            onClick={onRowClick ? () => onRowClick(row.original) : undefined}
            style={onRowClick ? { cursor: "pointer" } : undefined}
          >
            {row.getVisibleCells().map((cell) => (
              <td key={cell.id}>{flexRender(cell.column.columnDef.cell, cell.getContext())}</td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 6: Rodar teste e confirmar passagem**

Run: (mesmo comando do Step 4)
Expected: PASS (3 testes)

- [ ] **Step 7: Commit**

```bash
git add packages/frontend/src/components/DataTable.tsx packages/frontend/src/components/DataTable.test.tsx packages/frontend/package.json apps/web/package.json apps/web/package-lock.json
git commit -m "feat(frontend): crie DataTable generico sobre tanstack table"
```

---

## Task 9: Schema Zod + formulário de fazenda (RHF)

**Files:**
- Create: `apps/web/src/features/farms/schemas/farmSchema.ts`
- Create: `apps/web/src/features/farms/components/FarmForm.tsx`
- Test: `apps/web/src/features/farms/schemas/farmSchema.test.ts`
- Test: `apps/web/src/features/farms/components/FarmForm.test.tsx`

**Interfaces:**
- Consumes: `paths["/v1/farms"]["post"]["requestBody"]["content"]["application/json"]` (tipo gerado, Task 6).
- Produces: `export const farmFormSchema: z.ZodType<FarmFormValues>`, `export type FarmFormValues`; `export interface FarmFormProps` (`defaultValues?: Partial<FarmFormValues>`, `onSubmit: (values: FarmFormValues) => void`, `submitting: boolean`, `municipios: { ibgeCode: string; name: string; uf: string }[]`); `export function FarmForm(props: FarmFormProps): JSX.Element`.

- [ ] **Step 1: Instalar RHF + Zod**

Run: `cd apps/web && npm install react-hook-form zod @hookform/resolvers`

- [ ] **Step 2: Escrever teste do schema Zod**

```typescript
// apps/web/src/features/farms/schemas/farmSchema.test.ts
import { describe, expect, it } from "vitest";

import { farmFormSchema } from "./farmSchema";

describe("farmFormSchema", () => {
  it("aceita dados validos", () => {
    const result = farmFormSchema.safeParse({
      name: "Fazenda Boa Vista",
      uf: "GO",
      municipioIbgeCode: "5208707",
    });

    expect(result.success).toBe(true);
  });

  it("rejeita nome vazio", () => {
    const result = farmFormSchema.safeParse({ name: "", uf: "GO", municipioIbgeCode: "5208707" });

    expect(result.success).toBe(false);
  });

  it("rejeita UF com mais de 2 caracteres", () => {
    const result = farmFormSchema.safeParse({
      name: "Fazenda X",
      uf: "GOI",
      municipioIbgeCode: "5208707",
    });

    expect(result.success).toBe(false);
  });

  it("rejeita codigo IBGE fora do formato de 7 digitos", () => {
    const result = farmFormSchema.safeParse({
      name: "Fazenda X",
      uf: "GO",
      municipioIbgeCode: "123",
    });

    expect(result.success).toBe(false);
  });
});
```

- [ ] **Step 3: Rodar teste e confirmar falha**

Run: `cd apps/web && npx vitest run src/features/farms/schemas/farmSchema.test.ts`
Expected: FAIL com `Cannot find module './farmSchema'`

- [ ] **Step 4: Implementar `farmSchema.ts`**

```typescript
// apps/web/src/features/farms/schemas/farmSchema.ts
import { z } from "zod";

export const farmFormSchema = z.object({
  name: z.string().trim().min(2, "Nome deve ter ao menos 2 caracteres."),
  uf: z.string().length(2, "UF deve ter 2 letras."),
  municipioIbgeCode: z.string().regex(/^\d{7}$/, "Codigo IBGE deve ter 7 digitos."),
});

export type FarmFormValues = z.infer<typeof farmFormSchema>;
```

- [ ] **Step 5: Rodar teste e confirmar passagem**

Run: `cd apps/web && npx vitest run src/features/farms/schemas/farmSchema.test.ts`
Expected: PASS (4 testes)

- [ ] **Step 6: Escrever teste do formulário**

```tsx
// apps/web/src/features/farms/components/FarmForm.test.tsx
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { FarmForm } from "./FarmForm";

const municipios = [{ ibgeCode: "5208707", name: "Goiania", uf: "GO" }];

describe("FarmForm", () => {
  it("envia dados validos", async () => {
    const onSubmit = vi.fn();
    render(<FarmForm municipios={municipios} onSubmit={onSubmit} submitting={false} />);

    fireEvent.change(screen.getByLabelText(/nome/i), { target: { value: "Fazenda Boa Vista" } });
    fireEvent.change(screen.getByLabelText(/uf/i), { target: { value: "GO" } });
    fireEvent.change(screen.getByLabelText(/munic[ií]pio/i), { target: { value: "5208707" } });
    fireEvent.click(screen.getByRole("button", { name: /salvar/i }));

    await waitFor(() => {
      expect(onSubmit).toHaveBeenCalledWith({
        name: "Fazenda Boa Vista",
        uf: "GO",
        municipioIbgeCode: "5208707",
      });
    });
  });

  it("mostra erro de validacao e preserva o valor digitado", async () => {
    const onSubmit = vi.fn();
    render(<FarmForm municipios={municipios} onSubmit={onSubmit} submitting={false} />);

    fireEvent.change(screen.getByLabelText(/nome/i), { target: { value: "A" } });
    fireEvent.click(screen.getByRole("button", { name: /salvar/i }));

    await waitFor(() => {
      expect(screen.getByText(/ao menos 2 caracteres/i)).toBeInTheDocument();
    });
    expect(screen.getByLabelText(/nome/i)).toHaveValue("A");
    expect(onSubmit).not.toHaveBeenCalled();
  });

  it("desabilita o botao salvar durante submitting", () => {
    render(<FarmForm municipios={municipios} onSubmit={vi.fn()} submitting />);

    expect(screen.getByRole("button", { name: /salvar/i })).toBeDisabled();
  });
});
```

- [ ] **Step 7: Rodar teste e confirmar falha**

Run: `cd apps/web && npx vitest run src/features/farms/components/FarmForm.test.tsx`
Expected: FAIL com `Cannot find module './FarmForm'`

- [ ] **Step 8: Implementar `FarmForm.tsx`**

```tsx
// apps/web/src/features/farms/components/FarmForm.tsx
import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";

import { farmFormSchema, type FarmFormValues } from "../schemas/farmSchema";

export interface FarmFormProps {
  defaultValues?: Partial<FarmFormValues>;
  onSubmit: (values: FarmFormValues) => void;
  submitting: boolean;
  municipios: { ibgeCode: string; name: string; uf: string }[];
}

export function FarmForm({ defaultValues, onSubmit, submitting, municipios }: FarmFormProps) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<FarmFormValues>({
    resolver: zodResolver(farmFormSchema),
    defaultValues: {
      name: defaultValues?.name ?? "",
      uf: defaultValues?.uf ?? "",
      municipioIbgeCode: defaultValues?.municipioIbgeCode ?? "",
    },
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} noValidate>
      <div>
        <label htmlFor="farm-name">Nome</label>
        <input id="farm-name" {...register("name")} aria-describedby="farm-name-error" />
        {errors.name && (
          <p id="farm-name-error" role="alert">
            {errors.name.message}
          </p>
        )}
      </div>

      <div>
        <label htmlFor="farm-uf">UF</label>
        <input id="farm-uf" {...register("uf")} aria-describedby="farm-uf-error" maxLength={2} />
        {errors.uf && (
          <p id="farm-uf-error" role="alert">
            {errors.uf.message}
          </p>
        )}
      </div>

      <div>
        <label htmlFor="farm-municipio">Municipio</label>
        <select id="farm-municipio" {...register("municipioIbgeCode")} aria-describedby="farm-municipio-error">
          <option value="">Selecione</option>
          {municipios.map((m) => (
            <option key={m.ibgeCode} value={m.ibgeCode}>
              {m.name} ({m.uf})
            </option>
          ))}
        </select>
        {errors.municipioIbgeCode && (
          <p id="farm-municipio-error" role="alert">
            {errors.municipioIbgeCode.message}
          </p>
        )}
      </div>

      <button type="submit" disabled={submitting}>
        {submitting ? "Salvando..." : "Salvar"}
      </button>
    </form>
  );
}
```

- [ ] **Step 9: Rodar teste e confirmar passagem**

Run: `cd apps/web && npx vitest run src/features/farms/components/FarmForm.test.tsx`
Expected: PASS (3 testes)

- [ ] **Step 10: Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json apps/web/src/features/farms/schemas apps/web/src/features/farms/components/FarmForm.tsx apps/web/src/features/farms/components/FarmForm.test.tsx
git commit -m "feat(farms): implemente schema zod e formulario com rhf"
```

---

## Task 10: `FarmsPage` — lista + DataTable + mutações + Sonner

**Files:**
- Create: `apps/web/src/features/farms/routes/FarmsPage.tsx`
- Create: `apps/web/src/features/farms/components/FarmColumns.tsx`
- Test: `apps/web/src/features/farms/routes/FarmsPage.test.tsx`
- Modify: `apps/web/src/App.tsx` (rota `/fazendas` com `React.lazy`)

**Interfaces:**
- Consumes: `$api` (Task 7), `DataTable` (Task 8), `FarmForm` (Task 9).
- Produces: `export function FarmsPage(): JSX.Element` — estados loading (Skeleton)/empty/error/success, `AlertDialog` de confirmação de arquivamento, toast de sucesso/erro.

- [ ] **Step 1: Instalar Sonner e componentes shadcn necessários**

Consultar `mcp__shadcn__search_items_in_registries` / `mcp__shadcn__get_add_command_for_items` para `sonner`, `alert-dialog`, `skeleton`, `sheet` (ou `dialog`), `table` — instalar via `npx shadcn add <componentes>` (comando exato retornado pela ferramenta shadcn MCP) dentro de `apps/web`.

- [ ] **Step 2: Escrever teste de `FarmsPage` cobrindo loading/empty/error/success**

```tsx
// apps/web/src/features/farms/routes/FarmsPage.test.tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen, waitFor } from "@testing-library/react";
import { describe, expect, it, vi, beforeEach } from "vitest";

import { $api } from "../../../lib/apiClient";
import { FarmsPage } from "./FarmsPage";

function renderWithClient(children: React.ReactNode) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={client}>{children}</QueryClientProvider>);
}

describe("FarmsPage", () => {
  beforeEach(() => {
    vi.restoreAllMocks();
  });

  it("mostra skeleton de loading e depois lista de fazendas", async () => {
    vi.spyOn($api, "useQuery").mockReturnValue({
      data: { items: [{ id: "1", name: "Fazenda A", uf: "GO", municipioName: "Goiania", archivedAt: null }] },
      isLoading: false,
      error: null,
    } as never);

    renderWithClient(<FarmsPage />);

    await waitFor(() => {
      expect(screen.getByText("Fazenda A")).toBeInTheDocument();
    });
  });

  it("mostra estado vazio quando nao ha fazendas", async () => {
    vi.spyOn($api, "useQuery").mockReturnValue({
      data: { items: [] },
      isLoading: false,
      error: null,
    } as never);

    renderWithClient(<FarmsPage />);

    await waitFor(() => {
      expect(screen.getByText(/nenhuma fazenda/i)).toBeInTheDocument();
    });
  });

  it("mostra estado de erro com retry", async () => {
    vi.spyOn($api, "useQuery").mockReturnValue({
      data: undefined,
      isLoading: false,
      error: new Error("falha"),
    } as never);

    renderWithClient(<FarmsPage />);

    await waitFor(() => {
      expect(screen.getByRole("button", { name: /tentar novamente/i })).toBeInTheDocument();
    });
  });
});
```

**Nota de implementação:** mockar `$api.useQuery` diretamente via `vi.spyOn` pode ser frágil dependendo de como `openapi-react-query` exporta o objeto `$api` (pode não ser mockável assim se for gerado via factory a cada import). Se o mock acima não funcionar, alternativa: usar `msw` (Mock Service Worker) para interceptar a chamada HTTP real, ou extrair a chamada de dados para um hook próprio (`useFarmsQuery`) que possa ser mockado por módulo (`vi.mock('../hooks/useFarmsQuery')`). Escolher a abordagem que funcionar ao rodar o teste; ajustar os 3 testes acima de acordo, mantendo a mesma cobertura (loading/empty/error/success).

- [ ] **Step 3: Rodar teste e confirmar falha**

Run: `cd apps/web && npx vitest run src/features/farms/routes/FarmsPage.test.tsx`
Expected: FAIL com `Cannot find module './FarmsPage'`

- [ ] **Step 4: Implementar `FarmColumns.tsx`**

```tsx
// apps/web/src/features/farms/components/FarmColumns.tsx
import type { ColumnDef } from "@tanstack/react-table";

export interface FarmRow {
  id: string;
  name: string;
  uf: string;
  municipioName: string;
  archivedAt: string | null;
}

export function buildFarmColumns(onArchive: (id: string) => void): ColumnDef<FarmRow, unknown>[] {
  return [
    { accessorKey: "name", header: "Nome" },
    { accessorKey: "uf", header: "UF" },
    { accessorKey: "municipioName", header: "Municipio" },
    {
      id: "status",
      header: "Status",
      cell: ({ row }) => (row.original.archivedAt ? "Arquivada" : "Ativa"),
    },
    {
      id: "actions",
      header: "Acoes",
      cell: ({ row }) =>
        row.original.archivedAt ? null : (
          <button type="button" onClick={() => onArchive(row.original.id)}>
            Arquivar
          </button>
        ),
    },
  ];
}
```

- [ ] **Step 5: Implementar `FarmsPage.tsx`**

Usar `$api.useQuery("get", "/v1/farms", {})` para listagem, `$api.useMutation("post", "/v1/farms")` para criar, `$api.useMutation("post", "/v1/farms/{farm_id}/archive")` para arquivar (conferir os nomes exatos de path/params gerados em `api-types.ts` após a Task 6 — ajustar as chamadas conforme o schema real). Estrutura:

```tsx
// apps/web/src/features/farms/routes/FarmsPage.tsx
import { useState } from "react";
import { toast } from "sonner";

import { DataTable } from "@safraos/frontend/components/DataTable";
import { $api } from "../../../lib/apiClient";
import { FarmForm, type FarmFormValues } from "../components/FarmForm";
import { buildFarmColumns } from "../components/FarmColumns";

export function FarmsPage() {
  const [showForm, setShowForm] = useState(false);
  const [farmToArchive, setFarmToArchive] = useState<string | null>(null);

  const farmsQuery = $api.useQuery("get", "/v1/farms", {});
  const municipiosQuery = $api.useQuery("get", "/v1/municipios", {});

  const createMutation = $api.useMutation("post", "/v1/farms", {
    onSuccess: () => {
      toast.success("Fazenda criada com sucesso.");
      setShowForm(false);
      farmsQuery.refetch();
    },
    onError: (error: unknown) => {
      toast.error("Nao foi possivel criar a fazenda.");
    },
  });

  const archiveMutation = $api.useMutation("post", "/v1/farms/{farm_id}/archive", {
    onSuccess: () => {
      toast.success("Fazenda arquivada.");
      setFarmToArchive(null);
      farmsQuery.refetch();
    },
    onError: () => {
      toast.error("Nao foi possivel arquivar a fazenda.");
    },
  });

  if (farmsQuery.isLoading) {
    return <p role="status">Carregando fazendas...</p>;
  }

  if (farmsQuery.error) {
    return (
      <div role="alert">
        <p>Erro ao carregar fazendas.</p>
        <button type="button" onClick={() => farmsQuery.refetch()}>
          Tentar novamente
        </button>
      </div>
    );
  }

  const farms = farmsQuery.data?.items ?? [];

  return (
    <section aria-labelledby="farms-heading">
      <h1 id="farms-heading">Fazendas</h1>
      <button type="button" onClick={() => setShowForm(true)}>
        Nova fazenda
      </button>

      {showForm && (
        <FarmForm
          municipios={(municipiosQuery.data?.items ?? []).map((m) => ({
            ibgeCode: m.ibgeCode,
            name: m.name,
            uf: m.uf,
          }))}
          submitting={createMutation.isPending}
          onSubmit={(values: FarmFormValues) =>
            createMutation.mutate({
              body: { name: values.name, uf: values.uf, municipioIbgeCode: values.municipioIbgeCode },
            })
          }
        />
      )}

      {farms.length === 0 ? (
        <p>Nenhuma fazenda cadastrada.</p>
      ) : (
        <DataTable
          columns={buildFarmColumns((id) => setFarmToArchive(id))}
          data={farms}
          emptyMessage="Nenhuma fazenda cadastrada."
          getRowId={(farm) => farm.id}
        />
      )}

      {farmToArchive && (
        <div role="alertdialog" aria-labelledby="archive-confirm-heading">
          <h2 id="archive-confirm-heading">Arquivar fazenda?</h2>
          <button type="button" onClick={() => setFarmToArchive(null)}>
            Cancelar
          </button>
          <button
            type="button"
            onClick={() =>
              archiveMutation.mutate({ params: { path: { farm_id: farmToArchive } } })
            }
          >
            Confirmar arquivamento
          </button>
        </div>
      )}
    </section>
  );
}
```

**Nota de implementação:** a rota `/v1/municipios` (listagem de municípios pro `<select>`) **não foi criada em nenhuma task anterior** — decisão de implementação: adicionar um endpoint simples `GET /v1/municipios` no router de farms (Task 5) que lista `municipios` sem tenant/RLS (dado de referência), ou revisar este plano antes de chegar na Task 10 para incluí-lo explicitamente na Task 5. **Ajustar a Task 5 para incluir esse endpoint antes de implementar a Task 10** — ele é pré-requisito do formulário.

Trocar `<button onClick>` de arquivar/formulário/dialog por componentes shadcn instalados no Step 1 (`Sheet`/`Dialog`, `AlertDialog`, `Skeleton`) para cumprir `docs/FRONTEND.md` — o esqueleto acima é o mínimo funcional; refinar visualmente usando os componentes reais durante a implementação, mantendo os mesmos `role`/labels usados nos testes (ou atualizando os testes para os `role`s que os componentes shadcn geram, ex.: `AlertDialog` já tem `role="alertdialog"` nativo).

- [ ] **Step 6: Rodar teste e ajustar conforme a estratégia de mock escolhida no Step 2**

Run: `cd apps/web && npx vitest run src/features/farms/routes/FarmsPage.test.tsx`
Expected: PASS (3 testes)

- [ ] **Step 7: Adicionar rota `/fazendas` em `App.tsx` com lazy loading**

Ler `apps/web/src/App.tsx` e o setup de rotas existente (React Router, conforme `docs/FRONTEND.md` §2). Adicionar:

```tsx
const FarmsPage = React.lazy(() => import("./features/farms/routes/FarmsPage").then((m) => ({ default: m.FarmsPage })));

// dentro das <Routes>:
// <Route path="/fazendas" element={<Suspense fallback={<p>Carregando...</p>}><FarmsPage /></Suspense>} />
```

- [ ] **Step 8: Rodar suíte web completa**

Run: `cd apps/web && npx vitest run`
Expected: PASS, sem regressão

- [ ] **Step 9: Commit**

```bash
git add apps/web/src/features/farms/routes apps/web/src/features/farms/components/FarmColumns.tsx apps/web/src/App.tsx
git commit -m "feat(farms): implemente tela de listagem, criacao e arquivamento"
```

---

## Task 11: Endpoint `GET /v1/municipios` (pré-requisito descoberto na Task 10)

**Files:**
- Modify: `apps/api/src/safraos_api/modules/farms/repository.py`
- Modify: `apps/api/src/safraos_api/modules/farms/router.py`
- Modify: `tests/database/test_farms_repository.py`
- Modify: `tests/rules/api/test_farms_router.py`

**Interfaces:**
- Produces: `FarmRepository.list_municipios(self) -> list[MunicipioRow]`; dataclass `MunicipioRow` (`ibge_code`, `name`, `uf`); rota `GET /v1/farms/../v1/municipios` — **decisão de path**: como município é dado de referência global (não tenant-scoped), expor em `/v1/municipios` como router próprio (`apps/api/src/safraos_api/modules/farms/router.py` pode registrar as duas rotas `/v1/farms` e `/v1/municipios`, ou criar um segundo `APIRouter` no mesmo arquivo — decisão de implementação, manter no mesmo módulo `farms` por proximidade de domínio).

- [ ] **Step 1: Escrever teste de `list_municipios` no repository (sem tenant context)**

```python
# adicionar em tests/database/test_farms_repository.py
async def test_list_municipios_returns_seeded_reference_data():
    settings = Settings()
    repo = FarmRepository(create_async_engine(settings.app_database_url, pool_pre_ping=True))
    try:
        municipios = await repo.list_municipios()
        codes = {m.ibge_code for m in municipios}
        assert "5208707" in codes
    finally:
        await repo._engine.dispose()
```

- [ ] **Step 2: Rodar teste e confirmar falha**

Run: `python -m pytest tests/database/test_farms_repository.py -v -k list_municipios`
Expected: FAIL com `AttributeError: 'FarmRepository' object has no attribute 'list_municipios'`

- [ ] **Step 3: Implementar `list_municipios` no repository**

```python
# adicionar em apps/api/src/safraos_api/modules/farms/repository.py

@dataclass(frozen=True)
class MunicipioRow:
    ibge_code: str
    name: str
    uf: str


# dentro da classe FarmRepository:
    async def list_municipios(self) -> list[MunicipioRow]:
        async with self._engine.begin() as conn:
            result = await conn.execute(text("SELECT ibge_code, name, uf FROM municipios ORDER BY name"))
            return [MunicipioRow(ibge_code=r.ibge_code, name=r.name, uf=r.uf) for r in result]
```

- [ ] **Step 4: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/database/test_farms_repository.py -v -k list_municipios`
Expected: PASS

- [ ] **Step 5: Escrever teste do endpoint no router**

```python
# adicionar em tests/rules/api/test_farms_router.py
def test_list_municipios_does_not_require_active_organization():
    fake_repo = FakeFarmRepository()
    app = _build_app(fake_repo)
    client = TestClient(app)
    client.cookies.set("safraos_session", "fake-session")

    response = client.get("/v1/municipios")

    assert response.status_code == 200
```

Adicionar `list_municipios` (retornando lista fixa) no `FakeFarmRepository` do mesmo arquivo.

- [ ] **Step 6: Rodar teste e confirmar falha**

Run: `python -m pytest tests/rules/api/test_farms_router.py -v -k list_municipios`
Expected: FAIL com `404 Not Found`

- [ ] **Step 7: Implementar endpoint no router**

```python
# adicionar em apps/api/src/safraos_api/modules/farms/router.py

municipios_router = APIRouter(prefix="/v1/municipios", tags=["municipios"])


class MunicipioResponse(BaseModel):
    ibge_code: str = Field(alias="ibgeCode")
    name: str
    uf: str

    model_config = {"populate_by_name": True}


class MunicipioListResponse(BaseModel):
    items: list[MunicipioResponse]


@municipios_router.get("", response_model=MunicipioListResponse)
async def list_municipios_endpoint(user: CurrentUserDep, repo: RepositoryDep):
    if user is None:
        raise ProblemDetailError(status=401, title="Sessao invalida.", code="farms.unauthorized")
    rows = await repo.list_municipios()
    return MunicipioListResponse(
        items=[MunicipioResponse(ibgeCode=r.ibge_code, name=r.name, uf=r.uf) for r in rows]
    )
```

Registrar `municipios_router` em `main.py` junto do `router` de farms (`app.include_router(municipios_router)`).

- [ ] **Step 8: Rodar teste e confirmar passagem**

Run: `python -m pytest tests/rules/api/test_farms_router.py -v`
Expected: PASS

- [ ] **Step 9: Regenerar tipos OpenAPI (novo endpoint mudou o schema)**

Run: `npm run generate:openapi` (API rodando)
Expected: `apps/web/src/lib/api-types.ts` atualizado com `/v1/municipios`

- [ ] **Step 10: Rodar suíte de regras e typecheck completos**

Run: `python -m pytest tests/rules -v && npm run typecheck`
Expected: PASS

- [ ] **Step 11: Commit**

```bash
git add apps/api/src/safraos_api/modules/farms tests/database/test_farms_repository.py tests/rules/api/test_farms_router.py apps/web/src/lib/api-types.ts
git commit -m "feat(farms): exponha listagem de municipios para o formulario"
```

---

## Task 12: E2E de CRUD/arquivamento com dois tenants

**Files:**
- Create: `tests/e2e/farms.spec.ts`

**Interfaces:**
- Consumes: helper `createActiveUser` (padrão já usado em `tests/e2e/organizations.spec.ts` — ler esse arquivo primeiro e replicar/importar o helper, extraindo para um módulo compartilhado `tests/e2e/helpers/auth.ts` **apenas se a duplicação for grande**; senão, copiar o helper local como o próprio `organizations.spec.ts` já faz).

- [ ] **Step 1: Ler `tests/e2e/organizations.spec.ts` inteiro para copiar o padrão de dois `APIRequestContext`, criação/seleção de organização e helpers de Mailpit**

- [ ] **Step 2: Escrever o E2E**

```typescript
// tests/e2e/farms.spec.ts
import { expect, test } from "@playwright/test";

const API_PORT = process.env.API_PORT ?? "5183";
const API_BASE = `http://localhost:${API_PORT}`;

// Reaproveitar aqui o mesmo helper createActiveUser(email, password) de organizations.spec.ts,
// incluindo criacao+selecao de organizacao ativa via API antes de testar farms.

test("usuario cadastra, edita, arquiva fazenda por teclado; tenant alheio nao acessa", async ({ page, request }) => {
  // 1. Criar usuario A com organizacao ativa (via helper).
  // 2. page.goto('/fazendas'); usar Tab/Enter para navegar o formulario (teclado).
  // 3. Preencher nome, UF, municipio; submeter; esperar toast de sucesso e linha na tabela.
  // 4. Editar a fazenda criada; confirmar persistencia do novo nome.
  // 5. Arquivar via AlertDialog (teclado); confirmar que some da lista padrao e aparece com "include_archived".
  // 6. Criar usuario B com outra organizacao; via APIRequestContext, tentar GET/PUT/archive
  //    na fazenda do usuario A pelo id — esperar 404 problem+json.
  // Screenshot desktop do estado final da lista.
  await page.screenshot({ path: "tests/e2e/screenshots/farms-list.png" });
});
```

**Nota de implementação:** este teste é o mais dependente de decisões tomadas nas tasks anteriores (seletores exatos, texto de toast, `aria-label` do formulário). Escrever o corpo completo do teste durante a implementação da task, seguindo 1:1 o padrão de `organizations.spec.ts` (dois `APIRequestContext`, `page.getByRole('region', {name: ...})`, poll de Mailpit se necessário) e os `role`/`aria-label` reais que `FarmsPage`/`FarmForm` produzirem nas tasks 9-10 — não escrever seletores aqui que não foram confirmados nas tasks anteriores.

- [ ] **Step 3: Rodar E2E e confirmar passagem**

Run: `npm run test:e2e`
Expected: PASS, incluindo `health.spec.ts`, `identity.spec.ts`, `organizations.spec.ts`, `farms.spec.ts` — sem regressão

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/farms.spec.ts tests/e2e/screenshots
git commit -m "test(farms): adicione e2e de crud arquivamento e isolamento de tenant"
```

---

## Task 13: Atualizar documentação de entrega

**Files:**
- Modify: `docs/DEVELOPMENT.md`
- Modify: `docs/STATUS.md`

**Interfaces:** nenhuma (só documentação).

- [ ] **Step 1: Ler `docs/DEVELOPMENT.md` e `docs/STATUS.md` inteiros**

- [ ] **Step 2: Atualizar `docs/STATUS.md`**

Mover F4/issue #4 de `todo` para `doing` → depois de mergeado, o PI move para `finalizado` (o Code aplica só `proplan:done` na issue, conforme `CLAUDE.md`). Atualizar a linha do Índice Fatia ↔ SPEC (estado) e a seção "Agora".

- [ ] **Step 3: Atualizar `docs/DEVELOPMENT.md`**

Adicionar entrada da F4 com o que foi entregue (domínio, migration, API, frontend, E2E) seguindo o formato já usado para F1-F3 no mesmo arquivo.

- [ ] **Step 4: Commit**

```bash
git add docs/DEVELOPMENT.md docs/STATUS.md
git commit -m "docs(f4): registre progresso do cadastro de fazendas"
```

---

## Task 14: Validação final e PR

**Files:** nenhum arquivo novo — só comandos.

- [ ] **Step 1: Rodar suíte completa**

Run: `npm run lint && npm run typecheck && npm run test && npm run test:e2e`
Expected: tudo PASS

- [ ] **Step 2: Autorrevisão do diff completo contra `main`**

Run: `git diff main...HEAD --stat` e revisar arquivo por arquivo conforme `docs/REVIEW.md` (se existir) — achados P0/P1 bloqueiam, corrigir antes de abrir PR.

- [ ] **Step 3: Push e abrir PR**

```bash
git push -u origin issue-4-fazendas
gh pr create --repo RodReis/rrb-safraOS --base main --head issue-4-fazendas \
  --title "feat(fazendas): cadastro web de fazendas" \
  --body "$(cat <<'EOF'
## Problema

Owner nao tinha como cadastrar/consultar/alterar/arquivar fazendas do tenant ativo.

## Antes / depois

- Antes: sem entidade Farm; sem infra de cliente OpenAPI/TanStack Query/Table/RHF no frontend.
- Depois: CRUD+arquivamento de fazenda (nome, UF, municipio IBGE via seed), RLS por tenant, auditoria; nova infra de frontend (openapi-typescript+openapi-fetch+openapi-react-query, TanStack Query/Table, DataTable generico, RHF+Zod, Sonner) usada so por farms nesta fatia, documentada no design registrado em docs/superpowers/specs/2026-09-14-f4-fazendas-design.md — organizations (F3) nao foi retrofitada.

## Validacao executada

- npm run lint / typecheck / test / test:e2e — ok
- RLS cross-tenant testado em tests/database/test_farms_migration.py e test_farms_repository.py
- E2E com dois tenants e arquivamento por teclado

## Limitacoes

- Paginacao/ordenacao da tabela de fazendas e client-side nesta fatia (massa pequena); registrar se crescer.
- Seed de municipios cobre apenas os municipios usados nos exemplos da SPEC (GO/MT/MS); ampliar depois e reversivel.

refs #4

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: Aguardar CI**

Run: `gh pr checks <numero> --watch`
Expected: todos os jobs PASS

- [ ] **Step 5: Merge squash e confirmar**

```bash
gh pr merge <numero> --repo RodReis/rrb-safraOS --squash
gh pr view <numero> --repo RodReis/rrb-safraOS --json state,mergedAt,mergeCommit
```

- [ ] **Step 6: Comentário de encerramento na issue #4**

Usar a skill `fechar-card` (issue 4) — publicar Resumo/Aprendizado/Imprevistos antes de aplicar `proplan:done`.
